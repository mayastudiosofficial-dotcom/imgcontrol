import type {Metadata} from "next";import OptimizerClient from "@/components/OptimizerClient";export const metadata:Metadata={title:"Image Resizer — Resize Images by Width and Height",description:"Resize images by exact dimensions while preserving aspect ratio when needed.",alternates:{canonical:"/image-resizer"}};export default function Page(){return <><section className="pageHead"><div className="container"><div className="kicker">ImgControl · Resize</div><h1>Image Resizer</h1><p>Enter exact width and height, preserve aspect ratio, and choose your output format.</p></div></section><section className="section" style={{paddingTop:18}}><OptimizerClient mode="resize"/></section></>}
